{
  "name": "main",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"cd backend && npx prisma studio\"",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "setup": "cd backend && npx prisma generate && npx prisma migrate dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
