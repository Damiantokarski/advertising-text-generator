import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const viewPort = createSlice({
	name: "viewPort",
	initialState,
	reducers: {
	},
});

export const { } = viewPort.actions;
export default viewPort.reducer;
